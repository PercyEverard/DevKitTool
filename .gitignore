# ============================================================
# 大体积产物不入库（GitHub 单文件上限 100 MB，push 会被拒）
# 单文件二进制请通过 GitHub Releases 分发
# ============================================================

# 下载缓存 + 展开的 stage（约 1.1 GB）
devkit/build/

# 内嵌 payload（约 350 MB）
devkit/embed/

# 最终单文件二进制（约 355 MB）
devkit/devkit
